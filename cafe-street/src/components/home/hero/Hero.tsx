import React from "react";
import Header from "../header/Header";

const Hero = () => {
  return (
    <div className="relative h-screen w-full overflow-hidden px-[5vw] pt-[2vh]">
      <Header />
    </div>
  );
};

export default Hero;
